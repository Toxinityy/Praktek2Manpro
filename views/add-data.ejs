<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Data - SI Toko Retail</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../css/style.css">
    <script src="../js/script.js" defer></script>
    <script src="https://kit.fontawesome.com/142154afcd.js" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  </head>
  <style>
    body {
      background-color: #fff9e3;
    }
    * {
      font-family: 'Poppins', sans-serif;
    }

    .tabelPeople {
      display: none;
    }

    #drop-area {
      border: 2px dashed #ccc;
      border-radius: 8px;
      padding: 20px;
      text-align: center;
      font-family: Arial, sans-serif;
      margin: 20px;
    }

    #file-list {
      margin-top: 20px;
    }

    #file {
      width: 500px;
      height: 300px;
      color: grey;
      border: 4px solid;
      padding: 3rem;
      flex-direction: column;
      margin: auto;
      transform: translate(0, 50%);
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      background-color: #fff0bc;
    }
    label[id="file-upload-label"] {
      height: 200px;
      width: 400px;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      border: 2px dashed black;
      margin: 10px;
      background-color: #fff9e3;
      font-size: large;
    }
    div[class="d-flex justify-content-center"] {
      flex-direction: column;
    }
    #file-upload-label {
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    }

    label[for="file"]{
      color: black;
      font-weight: bold;
      font-size: large;
    }
    #file_upload {
      display: none; /* Hide the file input */
    }
  </style>
  <body>
    <%- include('navbar', { currentPage: 'add-data' }) %>
    <!-- Example of a form that Dropzone can take over -->
    <form id="file" method="POST" action="/upload" enctype="multipart/form-data" class="dropzone">
      <div id="text" class="d-flex justify-content-center">
        <label for="file">Drop CSV File Here!</label>
        <label for="file_upload" id="file-upload-label">Click here to browse</label>
        <input type="file" name="file_upload" id="file_upload" required>
      </div>
      <button name="upload" type="submit" id="upload">Upload</button>
    </form>

    <ul id="file-list"></ul>
  </body>
  <script>
    function displayFileName() {
      var fileInput = document.getElementById('file_upload');
      var fileName = document.getElementById('file-upload-label');

      fileName.textContent = fileInput.files[0] ? fileInput.files[0].name : 'Click here to browse';
    }
  </script>
</html>
<!-- addData.ejs -->
<% if (success) { %>
  <script>
    alert('File uploaded successfully!');
  </script>
<% } %>

